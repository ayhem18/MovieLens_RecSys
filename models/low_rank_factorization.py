
import torch
import numpy as np
import pandas as pd

from typing import Union

# class LowRankFac:
#     def __init__(self, 
#                  latent_dim: int, 
#                  A_matrix: Union[np.ndarray, pd.DataFrame]) -> None:
#         self.latent_dim = latent_dim
#         A_matrix = A_matrix.values if isinstance(A_matrix, pd.DataFrame) else A_matrix
#         if not long_matrix: 
#             m = A_matrix[(! np.isnan(A_matrix))]
#         pass

